version: '3'

services:
  openvpn:
    build: .
    tty: true
    stdin_open: true
    environment:
      - EXTERNAL_ADDRESS=87.20.169.83
    ports:
      - 1194:1194/udp
    cap_add:
      - NET_ADMIN
    volumes:
      - openvpn:/etc/openvpn
    restart: always

volumes:
  openvpn:
